# Copy to ops/vps/.env.github-actions (gitignored) then paste into
# GitHub → Afrika → Settings → Secrets and variables → Actions

VPS_HOST=153.75.235.176
VPS_USER=root
VPS_REPO_DIR=/opt/africa-insight

# Full private key contents (BEGIN…END), same key that can SSH to the VPS:
#   cat ~/.ssh/id_ed25519
VPS_SSH_KEY=
